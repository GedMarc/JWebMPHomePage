public class WebSocketImpl implements IWebSocketService<WebSocketImpl>
{
    public void onOpen(Session session, JWebMPSocket socket)
    {
    }
    public void onClose(Session session, JWebMPSocket socket)
    {
    }
    public void onMessage(String message, Session session, WebSocketMessageReceiver messageReceiver, JWebMPSocket socket)
    {
    }
    public void onError(Throwable t, JWebMPSocket socket)
    {
    }
    public boolean enabled()
    {
        return true;
    }
}
